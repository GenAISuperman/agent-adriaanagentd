apiVersion: apps/v1
kind: Deployment
metadata:
  name: "adriaanagentd"
  namespace: "agents-devteam"
  labels:
    app: "adriaanagentd"
    team: "devteam"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "adriaanagentd"
  template:
    metadata:
      labels:
        app: "adriaanagentd"
        team: "devteam"
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: "sa-adriaanagentd"
      containers:
        - name: "adriaanagentd"
          image: "acrshoppoc.azurecr.io/agents/adriaanagentd:latest"
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: "config-adriaanagentd"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
