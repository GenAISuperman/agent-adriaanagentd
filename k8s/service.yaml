apiVersion: v1
kind: Service
metadata:
  name: "adriaanagentd"
  namespace: "agents-devteam"
spec:
  selector:
    app: "adriaanagentd"
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
